## 3^x + 4^x = 9^x   ¿ Cuanto vale x ?

import math

for x in (1, 5, 0.25):
    
         a=(math.pow(3, x))
         b=(math.pow(4, x))
         c=(math.pow(9, x))
 
         print(a)
         print(b)
         print(c)
